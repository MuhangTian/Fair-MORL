import pandas as pd
import matplotlib.pyplot as plt

def line_graph(path, runs):
    return

def bar_chart(path, runs):
    return

def find_average(path, runs):
    for i in range(1, runs+1):
        df = pd.read_csv('Bandit_Qlearning/Q_learning_run_{}.csv'.format(i))
    return

if __name__ == "__main__":